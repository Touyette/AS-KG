Read `AGENTS.md`, then `INDEX.md`. That is the whole briefing.

`AGENTS.md` says what this repo is for, what to read, what *not* to read, and the
rules that bind regardless of what anyone asks. Do not go exploring first — a
previous session read most of the corpus before saying a word, which is the
specific mistake that file exists to prevent.

# Attic

`typed-graph.html` + `graph.js` were the first graph view: a force-directed
drawing of the whole graph, laid out by a parameter sweep. It stopped being
readable somewhere around 300 nodes and was replaced by the ego-centric
explorer (`scripts/explorer.*`). Kept only for reference; nothing builds it.
